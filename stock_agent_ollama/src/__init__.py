"""
Package initializer for the project src/ namespace.

This file makes `src` a regular Python package which helps imports like
`from src.rl import RLTrainer` work when running scripts from the project
root or other working directories.

If you prefer not to treat `src` as a package, you can run the app with
PYTHONPATH set to the project root (see README notes below).
"""

__all__ = []
